name: Build and Push Vote Image to Dhub and ECR

on:
  push:
    branches:
      - main
    paths:
      - 'evoting-new/vote/**'  # Ensure this is relative to the root directory
      - '.github/workflows/vote-to-dh-ecr.yml'

  pull_request:
    branches:
      - main
    paths:
      - 'evoting-new/vote/**'  # Ensure this is relative to the root directory
      - '.github/workflows/vote-to-dh-ecr.yml'

jobs:
  build_and_push_to_dockerhub:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ${{ github.workspace }}/evoting-new/vote  # Correct path for nested directory
          file: ${{ github.workspace }}/evoting-new/vote/Dockerfile  # Dockerfile path
          push: true
          tags: temple011/vote-repo:latest
